[project]
name = "stock-recommender"
version = "0.1.0"
description = "Telegram-based US stock evaluation bot."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2,<6.0",
]

[dependency-groups]
dev = []

[tool.uv]
package = false
